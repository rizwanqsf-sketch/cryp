{
  "name": "awaker-trade-root",
  "version": "1.0.0",
  "description": "Root package.json to deploy Awaker Trade on Hostinger",
  "scripts": {
    "start": "cd awaker-backend && node server.js",
    "build": "cd awaker-backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
